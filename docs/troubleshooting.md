# Troubleshooting

The detailed historical troubleshooting record is currently maintained in:

```text
docs/grasp.md
```

Key categories:

```text
rosbridge source / message md5 mismatch
MPC run mode / service availability
RealSense stream missing
TF missing during BASE lock
CUDA / PyTorch driver mismatch
YOLO false positives and highlight suppression
OCR/QR recognition instability
MPC path distortion and joint constraints
hand pressure threshold tuning
```

When a new issue is solved, add the short conclusion here and the detailed
procedure in `docs/grasp.md`.
